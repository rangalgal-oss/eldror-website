# Eldror — אתר תדמית (עבודות גובה בסנפלינג)

אתר סטטי (HTML בלבד) של חברת **אלדרור — עבודות גובה בסנפלינג**, מתארח ב‑GitHub Pages
ומוגש בדומיין [eldror.co.il](https://eldror.co.il) (ראו קובץ `CNAME`).
אין צד שרת ואין שלב בנייה — הקבצים מוגשים כמו שהם.

## מבנה האתר

| קובץ | תיאור |
|------|-------|
| `index.html` | דף הבית (רשת שירותים, גלריה, טופס יצירת קשר) |
| `itum-gag.html` | איטום גגות וחזיתות |
| `nikuy-hazitot.html` | ניקוי חזיתות בניינים |
| `tikun-nizkey-hedef.html` | תיקון נזקי הדף |
| `marzavim.html` | התקנת מרזבים (דף ראשי) |
| `marzavim-*.html` | דפי ערים למרזבים (תל אביב / רמת גן / גבעתיים / ראשון לציון) |
| `binyan-mesukan.html` | הסרת צו בניין מסוכן (דף ראשי) |
| `binyan-mesukan-*.html` | דפי ערים לבניין מסוכן (8 ערים) |

### קבצי SEO / תשתית
- `sitemap.xml`, `robots.txt` — אינדוקס למנועי חיפוש.
- `CNAME` — דומיין מותאם אישית (`eldror.co.il`).
- `llms.txt`, `llms-full.txt` — מידע עבור סורקי AI.
- `google7519440aba841fa6.html` — אימות בעלות מול Google Search Console.
- `gallery_photos/` — תמונות הגלריה והנכסים.

## איך מפעילים מקומית

האתר סטטי, אז כל שרת קבצים פשוט יספיק:

```bash
python3 -m http.server 8000
# פתחו http://localhost:8000
```

## פרסום (Deploy)

דחיפה (push) לענף שמחובר ל‑GitHub Pages מפרסמת את האתר אוטומטית ל‑`eldror.co.il`.

## יצירת קשר באתר

- **טלפון / וואטסאפ:** 0522-221-120
- קישורי טלפון בכל הדפים משתמשים בפורמט בינלאומי: `tel:+972522221120`
- קישורי וואטסאפ משתמשים ב‑`https://wa.me/972522221120` (מספר בינלאומי ללא אפס מוביל).
- טופס יצירת הקשר בדף הבית פותח וואטסאפ עם הודעה ממולאת מראש (אין שמירת לידים בשרת).

> הערה: `eldror_website.html` הוא גרסה ישנה של דף הבית שאינה בשימוש ומסומנת `noindex`.
